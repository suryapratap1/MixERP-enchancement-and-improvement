// ReSharper disable All
using System;
using System.Diagnostics;
using System.Linq;
using MixERP.Net.Api.Transactions.Fakes;
using MixERP.Net.ApplicationState.Cache;
using Xunit;

namespace MixERP.Net.Api.Transactions.Tests
{
    public class GetProductViewTests
    {
        public static GetProductViewController Fixture()
        {
            GetProductViewController controller = new GetProductViewController(new GetProductViewRepository(), "", new LoginView());
            return controller;
        }

        [Fact]
        [Conditional("Debug")]
        public void Execute()
        {
            var actual = Fixture().Execute(new GetProductViewController.Annotation());
            Assert.NotNull(actual);
        }
    }
}